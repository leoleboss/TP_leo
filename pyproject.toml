[project]
name = "medas-tp-financial-reporting"
version = "0.1.0"
description = "Reporting Excel automatisé"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "pyarrow",
    "openpyxl",
    "matplotlib",
]
